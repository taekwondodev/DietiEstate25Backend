secrets:
  # JWT secret and SMTP password are test-only values committed intentionally.
  # They are never used in production — production secrets are injected via environment variables.
  - id: generic-api-key
    paths:
      - backend/src/test/resources/application-test.properties
    statement: "Test-only credentials (JWT secret, SMTP password) — production secrets are env-var injected"

  # PostgreSQL credentials in the integration test compose file are fixed test values
  # used exclusively by the ephemeral Docker Compose test environment.
  - id: generic-api-key
    paths:
      - compose.test.yaml
    statement: "Test-only PostgreSQL credentials for ephemeral integration test environment"
